version: "3.9"
services:
  ats-quant:
    build: .
    container_name: ats-quant
    working_dir: /app
    volumes: [ "./:/app" ]
    env_file: [ ".env" ]
    environment: [ "TZ=UTC" ]
    restart: unless-stopped
